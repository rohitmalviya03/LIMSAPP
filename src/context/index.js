import React from "react";
import { LabProvider } from "./LabContext";

// Wrap your app's root (e.g., in App.js) with LabProvider
// Example:
// <LabProvider>
//   <App />
// </LabProvider>

export default LabProvider;
